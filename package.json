{
  "name": "azure-live-meeting-copilot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w shared && npm run build -w web",
    "lint": "npm run lint -w web",
    "typecheck": "npm run typecheck -w web"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
