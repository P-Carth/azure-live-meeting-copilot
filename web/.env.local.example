# ── Required: Azure OpenAI ────────────────────────────────────────────────────
AZURE_OPENAI_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com
AZURE_OPENAI_API_KEY=
MODEL_PRIMARY_DEPLOYMENT=gpt-5-nano
MODEL_FALLBACK_DEPLOYMENT=gpt-5-nano
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# ── Optional: Azure Speech (falls back to browser Web Speech API if not set) ──
NEXT_PUBLIC_AZURE_SPEECH_KEY=
NEXT_PUBLIC_AZURE_SPEECH_REGION=eastus2

# ── Optional: Cosmos DB (falls back to in-memory store if not set) ────────────
# Recommended for Vercel deployment so state persists across serverless instances
COSMOS_ENDPOINT=
COSMOS_KEY=
COSMOS_DATABASE=meeting-copilot
COSMOS_CONTAINER=meeting-state
